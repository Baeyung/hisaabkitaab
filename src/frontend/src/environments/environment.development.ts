export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
  // Empty: localhost is never apex, so dev keeps full app behavior.
  apexHosts: [] as string[],
  appOrigin: '',
};
